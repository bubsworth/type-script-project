let userName = 'Max';

// userName = 'Max';
userName = 'Max';

let userAge = 34;
let isValid = true;

//string, number, boolean

let userID: string | number = 'abc1'
userID = 123; 

// userID = true;

// let user: object;

let user: {
  name: string;
  age: number;
  isAdmin: boolean;
  id: string; //123 | 'abc' | true | false | null | undefined
};

//user = 'Max';

user = {
  name: 'Max',
  age: 34,
  isAdmin: true,
  id: 'abc' //123
}

// user = {};